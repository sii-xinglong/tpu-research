# Launch GPU resources for development with SkyPilot command:
#   sky launch gpu_resource.sky.yaml
workdir: .

file_mounts:
  ~/.ssh/id_ed25519: ~/.ssh/id_ed25519

resources:
  accelerators: $ACCELERATOR
  use_spot: True # The default instance is spot.

setup: |